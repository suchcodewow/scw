# DT-Orders

## Quickstart

```bash
kubectl apply -f https://suchcodewow.io/manifests/dtorders/deployall.yaml
```

### command reference

attach to a running pod

```bash
kubectl exec --stdin --tty shell-demo -- /bin/bash
```
